[project]
name = "pykuntur"
version = "0.1.0"
description = "Jheison Mayta's personal website"
readme = "README.md"
requires-python = ">=3.12.11"
dependencies = [
    "django==5.2.6",
    "dotenv>=0.9.9",
    "whitenoise>=6.11.0",
]

[dependency-groups]
dev = [
    "django-debug-toolbar>=6.0.0",
]
