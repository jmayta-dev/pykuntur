[project]
name = "pykuntur"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.11"
dependencies = [
    "django==5.2.6",
]

[dependency-groups]
dev = [
    "django-debug-toolbar>=6.0.0",
]
